@model PicklePlay.ViewModels.EditProfileModel
@{
    ViewData["Title"] = "Edit Profile - PicklePlay";
    Layout = "~/Views/Shared/_Layout.cshtml";

    // Malaysian states for dropdown
    var malaysianStates = new List<string>
    {
        "Johor", "Kedah", "Kelantan", "Kuala Lumpur", "Labuan", "Melaka", 
        "Negeri Sembilan", "Pahang", "Penang", "Perak", "Perlis", "Putrajaya", 
        "Sabah", "Sarawak", "Selangor", "Terengganu"
    };
}

@section Styles {
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #2E8B57;
            --secondary: #FF6B35;
            --accent: #1E5B32;
            --light: #F8F9FA;
            --dark: #343A40;
            --court: #E8F5E9;
        }

        body {
            background: #F8F9FA;
            background-size: cover;
            min-height: 100vh;
        }

        .page-header {
            background: linear-gradient(135deg, #2E8B57 0%, #1E5B32 100%);
            color: white;
            padding: 30px 0;
            border-radius: 0 0 20px 20px;
            margin-bottom: 30px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }

        .profile-header {
            display: flex;
            align-items: center;
            gap: 20px;
            flex-wrap: wrap;
        }

        .profile-header img {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            object-fit: cover;
            border: 3px solid #A1D336;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        }

        .form-container {
            max-width: 100%;
            background: #ffffff;
            border-radius: 16px;
            padding: 30px;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.08);
            margin-bottom: 25px;
            border: 1px solid rgba(46, 139, 87, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .form-container:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.12);
        }

        .section-title {
            color: #2E8B57;
            font-weight: 700;
            border-bottom: 2px solid #2E8B57;
            padding-bottom: 10px;
            margin-bottom: 20px;
            display: inline-block;
            font-size: 1.4rem;
        }

        .btn-save {
            background: #2E8B57;
            color: white;
            border: none;
            border-radius: 50px;
            padding: 12px 28px;
            font-weight: 600;
            transition: all 0.3s;
        }

        .btn-save:hover {
            background: #1E5B32;
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }

        .btn-edit {
            background: #FF6B35;
            color: white;
            border: none;
            border-radius: 50px;
            padding: 12px 28px;
            font-weight: 600;
            transition: all 0.3s;
        }

        .btn-edit:hover {
            background: #e55a2b;
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }

        .btn-cancel {
            background: #6c757d;
            color: white;
            border: none;
            border-radius: 50px;
            padding: 12px 28px;
            font-weight: 600;
            transition: all 0.3s;
        }

        .btn-cancel:hover {
            background: #5a6268;
            color: white;
            transform: translateY(-2px);
        }

        .btn-verify {
            background-color: #17a2b8;
            color: white;
            font-weight: bold;
            border: none;
            padding: 6px 12px;
            font-size: 0.875rem;
            border-radius: 20px;
        }

        .btn-verify:hover {
            background-color: #138496;
            color: white;
        }

        /* Form states */
        .form-control:read-only {
            background-color: #f8f9fa;
            border-color: #e9ecef;
            border-radius: 10px;
        }

        .form-control:focus {
            border-color: #2E8B57;
            box-shadow: 0 0 0 0.2rem rgba(46, 139, 87, 0.25);
            border-radius: 10px;
        }

        .edit-mode .form-control {
            background-color: white;
            border-color: #ced4da;
            border-radius: 10px;
        }

        /* Validation styles */
        .field-validation-error {
            color: #dc3545;
            font-size: 0.875rem;
            display: block;
            margin-top: 0.25rem;
        }

        .input-validation-error {
            border-color: #dc3545;
            box-shadow: 0 0 0 0.2rem rgba(220, 53, 69, 0.25);
        }

        .validation-summary-errors {
            color: #dc3545;
            background-color: #f8d7da;
            border: 1px solid #f5c6cb;
            border-radius: 12px;
            padding: 1rem 1.5rem;
            margin-bottom: 1.5rem;
        }

        .validation-summary-errors ul {
            margin-bottom: 0;
            padding-left: 1rem;
        }

        /* Email verification section */
        .email-verification {
            background-color: #e8f5e9;
            border: 1px solid #2E8B57;
            border-radius: 12px;
            padding: 15px;
            margin: 15px 0;
        }

        .email-verified {
            color: #28a745;
            font-weight: bold;
        }

        .email-pending {
            color: #ffc107;
            font-weight: bold;
        }

        /* Wallet + Communities box */
        .side-box {
            background: #fff;
            border-radius: 16px;
            padding: 25px;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.08);
            margin-bottom: 25px;
            border: 1px solid rgba(46, 139, 87, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .side-box:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.12);
        }

        .wallet-balance {
            font-size: 2.2rem;
            font-weight: bold;
            color: #2E8B57;
            text-align: center;
        }

        .wallet-overview {
            margin-top: 15px;
            text-align: center;
        }

        .wallet-overview p {
            margin-bottom: 8px;
            font-weight: 500;
        }

        .community-list li {
            background: #f8f9fa;
            padding: 12px 15px;
            border-radius: 10px;
            margin-bottom: 10px;
            transition: all 0.3s;
            border-left: 4px solid #2E8B57;
        }

        .community-list li:hover {
            background: #e8f5e9;
            transform: translateX(5px);
        }

        .community-list a {
            color: #333;
            text-decoration: none;
            font-weight: 500;
        }

        .community-list a:hover {
            color: #2E8B57;
        }

        /* Chart container */
        .chart-container {
            position: relative;
            height: 200px;
            width: 100%;
        }

        /* Password change button */
        .btn-password-change {
            background-color: #6c757d;
            color: white;
            border: none;
            padding: 12px 20px;
            width: 100%;
            border-radius: 10px;
            font-weight: 600;
            transition: all 0.3s;
        }

        .btn-password-change:hover {
            background-color: #5a6268;
            color: white;
        }

        .btn-password-change:disabled {
            background-color: #6c757d;
            color: white;
            opacity: 0.6;
            cursor: not-allowed;
            transform: none !important;
        }

        .btn-password-change-enabled {
            background-color: #2E8B57 !important;
            color: white !important;
        }

        .btn-password-change-enabled:hover {
            background-color: #1E5B32 !important;
            color: white !important;
            transform: translateY(-2px);
        }

        /* Profile image upload area */
        .profile-image-upload {
            position: relative;
            display: inline-block;
        }

        .profile-image-upload .edit-overlay {
            position: absolute;
            bottom: 5px;
            right: 5px;
            background: #2E8B57;
            color: white;
            border-radius: 50%;
            width: 35px;
            height: 35px;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            opacity: 0;
            transition: all 0.3s;
        }

        .profile-image-upload:hover .edit-overlay {
            opacity: 1;
        }

        /* Stats cards */
        .stats-card {
            text-align: center;
            padding: 20px 15px;
            border-radius: 12px;
            background: white;
            box-shadow: 0 5px 15px rgba(0,0,0,0.05);
            transition: all 0.3s;
            border: 1px solid rgba(46, 139, 87, 0.1);
        }

        .stats-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
        }

        .stats-card i {
            font-size: 2rem;
            margin-bottom: 15px;
            color: #2E8B57;
        }

        .stats-card h4 {
            font-weight: 700;
            color: #343A40;
            margin-bottom: 5px;
            font-size: 1.4rem;
        }

        .stats-card p {
            color: #6c757d;
            margin: 0;
            font-weight: 500;
        }

        /* Responsive design */
        @@media (max-width: 768px) {
            .profile-header {
                flex-direction: column;
                text-align: center;
                gap: 15px;
            }

            .profile-header img {
                width: 80px;
                height: 80px;
            }

            .button-group {
                flex-direction: column;
                gap: 10px;
            }

            .button-group .btn {
                width: 100%;
            }

            .form-container {
                padding: 20px 15px;
                margin: 10px 0;
            }

            .side-box {
                padding: 20px;
            }

            .wallet-balance {
                font-size: 1.8rem;
            }

            .container {
                padding: 0 10px;
            }

            .page-header {
                padding: 20px 0;
            }
        }

        @@media (max-width: 576px) {
            .profile-header img {
                width: 70px;
                height: 70px;
            }

            .form-container {
                padding: 15px 10px;
            }

            .btn-save,
            .btn-edit,
            .btn-cancel {
                padding: 10px 20px;
                font-size: 0.9rem;
            }

            .wallet-balance {
                font-size: 1.5rem;
            }
        }
    </style>
}

<!-- Page Header -->
<div class="page-header">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-md-8">
                <h1><i class="fas fa-user-edit me-3"></i>Edit Profile</h1>
                <p class="lead mb-0">Manage your personal information and preferences</p>
            </div>
            <div class="col-md-4 text-end">
                <a href="@Url.Action("Community", "Home")" class="btn btn-light btn-sm">
                    <i class="fas fa-arrow-left me-2"></i>Back to Community
                </a>
            </div>
        </div>
    </div>
</div>

<div class="container mt-4">
    <!-- Display Messages -->
    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="fas fa-check-circle me-2"></i>@TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <i class="fas fa-exclamation-circle me-2"></i>@TempData["ErrorMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    @if (TempData["InfoMessage"] != null)
    {
        <div class="alert alert-info alert-dismissible fade show" role="alert">
            <i class="fas fa-info-circle me-2"></i>@TempData["InfoMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <div class="row">
        <!-- Left side: Profile form -->
        <div class="col-lg-8 col-md-7">
            <div class="form-container">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h3 class="mb-0"><i class="fas fa-user-cog me-2"></i>Profile Settings</h3>
                    <button type="button" class="btn btn-edit" id="editToggleBtn">
                        <i class="fas fa-edit me-2"></i>Edit Profile
                    </button>
                </div>

                <!-- Profile Form -->
                <form method="post" asp-controller="Home" asp-action="UpdateProfile" enctype="multipart/form-data"
                    id="profileForm" class="needs-validation" novalidate>
                    @Html.AntiForgeryToken()
                    <input type="hidden" asp-for="UserId" />
                    <input type="hidden" asp-for="CurrentProfileImagePath" />

                    @Html.ValidationSummary(true, "", new { @class = "validation-summary-errors" })

                    <div class="profile-header mb-4">
                        <div class="profile-image-upload">
                            <img src="@(string.IsNullOrEmpty(Model.CurrentProfileImagePath) ? "/images/profile_image.jpg" : Model.CurrentProfileImagePath)"
                                alt="Profile Picture" id="profileImagePreview" class="shadow">
                            <div class="edit-overlay" onclick="document.getElementById('profileImage').click()">
                                <i class="fas fa-camera"></i>
                            </div>
                        </div>
                        <div class="flex-grow-1">
                            <h4 class="fw-bold mb-2" id="displayName">@Model.FullName</h4>
                            <p class="text-muted mb-2">Member since @DateTime.Now.Year</p>
                            <input type="file" class="form-control d-none" id="profileImage" name="ProfileImage"
                                accept="image/*" style="max-width: 300px;">
                            <small class="text-muted d-none" id="imageHelp">
                                <i class="fas fa-info-circle me-1"></i>Max file size: 5MB. Supported formats: JPG, PNG, GIF
                            </small>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label asp-for="FullName" class="form-label fw-bold">
                                <i class="fas fa-user me-2"></i>Full Name
                            </label>
                            <input asp-for="FullName" class="form-control" readonly
                                data-original-value="@Model.FullName" />
                            <span asp-validation-for="FullName" class="field-validation-error"></span>
                        </div>

                        <div class="col-md-6 mb-3">
                            <label asp-for="Email" class="form-label fw-bold">
                                <i class="fas fa-envelope me-2"></i>Email Address
                            </label>
                            <div class="input-group">
                                <input asp-for="Email" class="form-control" readonly
                                    data-original-value="@Model.Email" />
                                <span class="input-group-text bg-success text-white">
                                    <i class="fas fa-check me-1"></i>Verified
                                </span>
                            </div>
                            <small class="text-muted">Email cannot be changed</small>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label asp-for="PhoneNumber" class="form-label fw-bold">
                                <i class="fas fa-phone me-2"></i>Phone Number
                            </label>
                            <input asp-for="PhoneNumber" class="form-control" placeholder="e.g., +6012-3456789" readonly
                                data-original-value="@Model.PhoneNumber" pattern="^(\+?6?01)[0-46-9]-*[0-9]{7,8}$"
                                title="Please enter a valid Malaysian phone number (e.g., +6012-3456789 or 012-3456789)" />
                            <span asp-validation-for="PhoneNumber" class="field-validation-error"></span>
                            <small class="text-muted">Format: +6012-3456789 or 012-3456789</small>
                        </div>

                        <div class="col-md-6 mb-3">
                            <label asp-for="Gender" class="form-label fw-bold">
                                <i class="fas fa-venus-mars me-2"></i>Gender
                            </label>
                            <select asp-for="Gender" class="form-control" disabled data-original-value="@Model.Gender">
                                <option value="">Select your gender</option>
                                <option value="male">Male</option>
                                <option value="female">Female</option>
                                <option value="other">Other</option>
                            </select>
                            <span asp-validation-for="Gender" class="field-validation-error"></span>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label asp-for="DateOfBirth" class="form-label fw-bold">
                                <i class="fas fa-birthday-cake me-2"></i>Date of Birth
                            </label>
                            <input asp-for="DateOfBirth" type="date" class="form-control" readonly
                                data-original-value="@Model.DateOfBirth?.ToString("yyyy-MM-dd")"
                                max="@DateTime.Now.AddYears(-18).ToString("yyyy-MM-dd")"
                                title="You must be at least 18 years old" />
                            <span asp-validation-for="DateOfBirth" class="field-validation-error"></span>
                        </div>

                        <div class="col-md-6 mb-3">
                            <label asp-for="Age" class="form-label fw-bold">Age</label>
                            <input asp-for="Age" class="form-control" readonly />
                        </div>
                    </div>

                    <!-- Location and Password Change Section -->
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label asp-for="Location" class="form-label fw-bold">
                                <i class="fas fa-map-marker-alt me-2"></i>Location (State)
                            </label>
                            <select asp-for="Location" class="form-control" disabled
                                data-original-value="@Model.Location">
                                <option value="">Select your state</option>
                                @foreach (var state in malaysianStates)
                                {
                                    <option value="@state">@state</option>
                                }
                            </select>
                            <span asp-validation-for="Location" class="field-validation-error"></span>
                        </div>

                        <div class="col-md-6 mb-3">
                            <label class="form-label fw-bold">
                                <i class="fas fa-lock me-2"></i>Password
                            </label>
                            <div class="d-grid">
                                <button type="button" class="btn btn-password-change" id="changePasswordBtn"
                                    data-bs-toggle="modal" data-bs-target="#changePasswordModal" disabled>
                                    <i class="fas fa-key me-2"></i>Change Password
                                </button>
                            </div>
                            <small class="text-muted">Click "Edit Profile" to enable password change</small>
                        </div>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Bio" class="form-label fw-bold">
                            <i class="fas fa-edit me-2"></i>Bio
                        </label>
                        <textarea asp-for="Bio" class="form-control" rows="3"
                            placeholder="Tell something about yourself..." readonly data-original-value="@Model.Bio"
                            maxlength="500"></textarea>
                        <span asp-validation-for="Bio" class="field-validation-error"></span>
                        <small class="text-muted"><span id="bioCharCount">@(Model.Bio?.Length ?? 0)</span>/500
                            characters</small>
                    </div>

                    <!-- Action Buttons -->
                    <div class="d-flex gap-2 button-group mt-4">
                        <button type="submit" class="btn btn-save d-none" id="saveBtn">
                            <i class="fas fa-save me-2"></i>Save Changes
                        </button>
                        <button type="button" class="btn btn-cancel d-none" id="cancelBtn">
                            <i class="fas fa-times me-2"></i>Cancel
                        </button>
                    </div>
                </form>

                <div class="mt-4">
                    @await Component.InvokeAsync("EndorsementSection", new { userId = Model.UserId })
                </div>
            </div>

            
        </div>

        <!-- Right side: Wallet + Communities -->
        <div class="col-lg-4 col-md-5">
            <!-- Wallet Section -->
            <div class="side-box text-center">
                <h5 class="mb-3"><i class="fas fa-wallet me-2"></i>Wallet Balance & Overview</h5>
                <div class="chart-container">
                    <canvas id="walletChart"></canvas>
                </div>

                <!-- Legend -->
                <div class="mt-3">
                    <span class="badge bg-success me-1">Balance</span>
                    <span class="badge bg-primary me-1">Escrow</span>
                    <span class="badge bg-danger">Spent</span>
                </div>
                <div class="wallet-balance mt-2">RM 250.00</div>
                <div class="wallet-overview text-muted">
                    <p class="mb-1">Total Escrow: RM 120.00</p>
                    <p class="mb-2">Total Spent: RM 80.00</p>
                    <a href="@Url.Action("WalletManagement", "Wallet")" class="btn btn-save mt-2">
                        <i class="fas fa-arrow-right me-2"></i>Go to Wallet Management
                    </a>
                </div>
            </div>

            <!-- Communities Section -->
            <div class="side-box">
                <h5 class="mb-3"><i class="fas fa-users me-2"></i>Communities Joined</h5>
                <ul class="community-list list-unstyled">
                    <li>
                        <a href="@Url.Action("CommunityPages", "Home")" class="text-decoration-none">
                            🏸 KL Pickleball Club
                        </a>
                    </li>
                    <li>🎾 Penang Pickleballers</li>
                    <li>⚡ Weekend Warriors</li>
                </ul>
                <div class="text-center mt-3">
                    <a href="@Url.Action("Community", "Home")" class="btn btn-outline-primary btn-sm">
                        <i class="fas fa-eye me-2"></i>View All Communities
                    </a>
                </div>
            </div>

            <!-- Quick Stats -->
            <div class="side-box">
                <h5 class="mb-3"><i class="fas fa-chart-line me-2"></i>Quick Stats</h5>
                <div class="row">
                    <div class="col-6 mb-3">
                        <div class="stats-card">
                            <i class="fas fa-calendar-check"></i>
                            <h4>12</h4>
                            <p>Bookings</p>
                        </div>
                    </div>
                    <div class="col-6 mb-3">
                        <div class="stats-card">
                            <i class="fas fa-trophy"></i>
                            <h4>8</h4>
                            <p>Matches</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Change Password Modal -->
<div class="modal fade" id="changePasswordModal" tabindex="-1" aria-labelledby="changePasswordModalLabel"
    aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="changePasswordModalLabel">
                    <i class="fas fa-key me-2"></i>Change Password
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <form id="changePasswordForm" method="post" asp-controller="Auth" asp-action="ChangePassword">
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="email" value="@Model.Email" />

                    <div class="mb-3">
                        <label for="currentPassword" class="form-label fw-bold">Current Password</label>
                        <input type="password" class="form-control" id="currentPassword" name="currentPassword"
                            required>
                        <div class="invalid-feedback">Please enter your current password</div>
                    </div>

                    <div class="mb-3">
                        <label for="newPassword" class="form-label fw-bold">New Password</label>
                        <input type="password" class="form-control" id="newPassword" name="newPassword" minlength="6"
                            required>
                        <div class="invalid-feedback">Password must be at least 6 characters long</div>
                    </div>

                    <div class="mb-3">
                        <label for="confirmPassword" class="form-label fw-bold">Confirm New Password</label>
                        <input type="password" class="form-control" id="confirmPassword" name="confirmPassword"
                            required>
                        <div class="invalid-feedback">Passwords do not match</div>
                    </div>

                    <div class="alert alert-info">
                        <small>
                            <strong>Password Requirements:</strong><br>
                            • Minimum 6 characters
                        </small>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-save" id="submitPasswordChange">Change Password</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chartjs-plugin-datalabels@2.2.0"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            // Initialize wallet chart
            const ctx = document.getElementById('walletChart').getContext('2d');
            const walletChart = new Chart(ctx, {
                type: 'doughnut',
                data: {
                    labels: ['Balance', 'Escrow', 'Spent'],
                    datasets: [{
                        data: [250, 120, 80],
                        backgroundColor: [
                            'rgba(25, 135, 84, 0.8)',
                            'rgba(13, 110, 253, 0.8)',
                            'rgba(220, 53, 69, 0.8)'
                        ],
                        borderColor: [
                            'rgba(25, 135, 84, 1)',
                            'rgba(13, 110, 253, 1)',
                            'rgba(220, 53, 69, 1)'
                        ],
                        borderWidth: 1
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: { display: false },
                        datalabels: {
                            color: '#fff',
                            font: {
                                weight: 'bold',
                                size: 12
                            },
                            formatter: (value, ctx) => {
                                let label = ctx.chart.data.labels[ctx.dataIndex];
                                return label + "\nRM " + value;
                            }
                        }
                    }
                },
                plugins: [ChartDataLabels]
            });

            const editToggleBtn = document.getElementById('editToggleBtn');
            const saveBtn = document.getElementById('saveBtn');
            const cancelBtn = document.getElementById('cancelBtn');
            const form = document.getElementById('profileForm');
            const inputs = form.querySelectorAll('input, select, textarea');
            const profileImageInput = document.getElementById('profileImage');
            const imageHelp = document.getElementById('imageHelp');
            const bioTextarea = document.getElementById('Bio');
            const bioCharCount = document.getElementById('bioCharCount');
            const fullNameInput = document.getElementById('FullName');
            const locationSelect = document.querySelector('select[name="Location"]');
            const changePasswordBtn = document.getElementById('changePasswordBtn');

            // Store original values for cancel functionality
            const originalValues = {};
            inputs.forEach(input => {
                if (input.type !== 'file') {
                    originalValues[input.name] = input.value;
                }
            });

            // Edit mode toggle
            // Edit mode toggle
            editToggleBtn.addEventListener('click', function () {
                form.classList.add('edit-mode');
                inputs.forEach(input => {
                    if (input.id !== 'Age' && input.name !== 'Email') { // Keep age and email readonly
                        input.readOnly = false;
                    }
                    if (input.tagName === 'SELECT') {
                        input.disabled = false;
                    }
                });

                // Show file input
                profileImageInput.classList.remove('d-none');
                imageHelp.classList.remove('d-none');

                // Show action buttons
                saveBtn.classList.remove('d-none');
                cancelBtn.classList.remove('d-none');
                editToggleBtn.classList.add('d-none');

                // ✅ FIX: Enable the Change Password button
                changePasswordBtn.disabled = false;
                changePasswordBtn.classList.add('btn-password-change-enabled');
            });


            // Cancel edit
            cancelBtn.addEventListener('click', function () {
                form.classList.remove('edit-mode');
                inputs.forEach(input => {
                    input.readOnly = true;
                    if (input.tagName === 'SELECT') {
                        input.disabled = true;
                    }
                    // Restore original values
                    if (originalValues[input.name] !== undefined) {
                        input.value = originalValues[input.name];
                    }
                });

                // Hide file input and reset
                profileImageInput.classList.add('d-none');
                imageHelp.classList.add('d-none');
                profileImageInput.value = '';

                // Hide action buttons
                saveBtn.classList.add('d-none');
                cancelBtn.classList.add('d-none');
                editToggleBtn.classList.remove('d-none');

                changePasswordBtn.disabled = true;
                changePasswordBtn.classList.remove('btn-password-change-enabled');

                // Clear validation errors
                clearValidationErrors();
                updateBioCharCount();
            });

            // Bio character counter
            function updateBioCharCount() {
                if (bioTextarea) {
                    const charCount = bioTextarea.value.length;
                    bioCharCount.textContent = charCount;
                }
            }

            if (bioTextarea) {
                bioTextarea.addEventListener('input', updateBioCharCount);
            }

            // Real-time validation
            inputs.forEach(input => {
                if (input.type !== 'file') {
                    input.addEventListener('blur', function () {
                        validateField(this);
                    });

                    input.addEventListener('input', function () {
                        // Clear error when user starts typing
                        clearFieldError(this);
                    });
                }
            });

            // Clear field error
            function clearFieldError(field) {
                field.classList.remove('input-validation-error');
                let errorSpan = getErrorSpan(field);
                if (errorSpan) {
                    errorSpan.style.display = 'none';
                }
            }

            // Get error span for a field
            function getErrorSpan(field) {
                let errorSpan = field.nextElementSibling;
                if (!errorSpan || !errorSpan.classList.contains('field-validation-error')) {
                    errorSpan = field.parentNode.querySelector('.field-validation-error');
                }
                return errorSpan;
            }

            // Field validation function
            function validateField(field) {
                // Clear previous validation
                clearFieldError(field);

                let errorSpan = getErrorSpan(field);
                let isValid = true;
                let errorMessage = '';

                // Full Name validation - ALWAYS required when in edit mode
                if (field.name === 'FullName') {
                    if (!field.value.trim()) {
                        errorMessage = 'Full name is required.';
                        isValid = false;
                    } else if (field.value.trim().length < 2) {
                        errorMessage = 'Full name must be at least 2 characters long.';
                        isValid = false;
                    } else if (field.value.trim().length > 100) {
                        errorMessage = 'Full name cannot exceed 100 characters.';
                        isValid = false;
                    }
                }
                // Phone number validation
                else if (field.name === 'PhoneNumber' && field.value.trim()) {
                    const phoneRegex = /^(\+?6?01)[0-46-9]-*[0-9]{7,8}$/;
                    if (!phoneRegex.test(field.value.trim())) {
                        errorMessage = 'Please enter a valid Malaysian phone number (e.g., +6012-3456789 or 012-3456789)';
                        isValid = false;
                    }
                }
                // Phone number is empty - THIS IS ALLOWED, so no error
                else if (field.name === 'PhoneNumber' && !field.value.trim()) {
                    // Empty phone number is allowed, so no validation error
                    isValid = true;
                }
                // Date of birth validation (must be 18+)
                else if (field.type === 'date' && field.value) {
                    const dob = new Date(field.value);
                    const today = new Date();
                    let age = today.getFullYear() - dob.getFullYear();
                    const monthDiff = today.getMonth() - dob.getMonth();

                    if (monthDiff < 0 || (monthDiff === 0 && today.getDate() < dob.getDate())) {
                        age--;
                    }

                    if (age < 18) {
                        errorMessage = 'You must be at least 18 years old.';
                        isValid = false;
                    }
                }
                // Bio length validation
                else if (field.name === 'Bio' && field.value.length > 500) {
                    errorMessage = 'Bio cannot exceed 500 characters.';
                    isValid = false;
                }
                // Location validation
                else if (field.name === 'Location' && field.value && field.value.length > 100) {
                    errorMessage = 'Location cannot exceed 100 characters.';
                    isValid = false;
                }

                // Show error if invalid
                if (!isValid) {
                    field.classList.add('input-validation-error');
                    if (errorSpan) {
                        errorSpan.textContent = errorMessage;
                        errorSpan.style.display = 'block';
                    }
                }

                return isValid;
            }

            // Clear all validation errors
            function clearValidationErrors() {
                inputs.forEach(input => {
                    clearFieldError(input);
                });
            }

            // Form submission validation - STRICT validation
            form.addEventListener('submit', function (e) {
                let isFormValid = true;

                // Validate all editable fields
                inputs.forEach(input => {
                    if (input.type !== 'file' && !input.readOnly) {
                        if (!validateField(input)) {
                            isFormValid = false;
                        }
                    }
                });

                // SPECIAL CHECK: FullName must not be empty
                if (fullNameInput && !fullNameInput.readOnly) {
                    if (!fullNameInput.value.trim()) {
                        validateField(fullNameInput); // This will show the error
                        isFormValid = false;
                    }
                }

                if (!isFormValid) {
                    e.preventDefault();
                    e.stopPropagation(); // Important: Stop the form from submitting

                    // Scroll to first error
                    const firstError = form.querySelector('.input-validation-error');
                    if (firstError) {
                        firstError.scrollIntoView({
                            behavior: 'smooth',
                            block: 'center'
                        });
                        firstError.focus(); // Focus on the first error field
                    }

                    return false;
                }

                // If we get here, form is valid and will submit
                console.log('Form is valid, submitting...');
            });

            // Profile image preview
            if (profileImageInput) {
                profileImageInput.addEventListener('change', function () {
                    if (this.files && this.files[0]) {
                        // Validate file type
                        const allowedTypes = ['image/jpeg', 'image/jpg', 'image/png', 'image/gif'];
                        if (!allowedTypes.includes(this.files[0].type)) {
                            alert('Please select a valid image file (JPG, PNG, GIF).');
                            this.value = '';
                            return;
                        }

                        // Validate file size (5MB)
                        if (this.files[0].size > 5 * 1024 * 1024) {
                            alert('File size must be less than 5MB.');
                            this.value = '';
                            return;
                        }

                        const reader = new FileReader();
                        reader.onload = function (e) {
                            document.getElementById('profileImagePreview').src = e.target.result;
                        }
                        reader.readAsDataURL(this.files[0]);
                    }
                });
            }

            // Age calculation from date of birth
            const dobInput = document.getElementById('DateOfBirth');
            const ageInput = document.getElementById('Age');
            if (dobInput && ageInput) {
                dobInput.addEventListener('change', function () {
                    if (this.value) {
                        const dob = new Date(this.value);
                        const today = new Date();
                        let age = today.getFullYear() - dob.getFullYear();
                        const monthDiff = today.getMonth() - dob.getMonth();

                        if (monthDiff < 0 || (monthDiff === 0 && today.getDate() < dob.getDate())) {
                            age--;
                        }

                        ageInput.value = age > 0 ? age : '';
                    } else {
                        ageInput.value = '';
                    }
                });
            }

            // Additional: Prevent form submission on Enter key in readonly mode
            form.addEventListener('keydown', function (e) {
                if (e.key === 'Enter' && !form.classList.contains('edit-mode')) {
                    e.preventDefault();
                }
            });

            // Password Change Modal Functionality
            @* document.getElementById('submitPasswordChange').addEventListener('click', function () {
                const form = document.getElementById('changePasswordForm');
                const currentPassword = document.getElementById('currentPassword');
                const newPassword = document.getElementById('newPassword');
                const confirmPassword = document.getElementById('confirmPassword');

                let isValid = true;

                // Reset validation
                [currentPassword, newPassword, confirmPassword].forEach(input => {
                    input.classList.remove('is-invalid');
                });

                // Validate current password
                if (!currentPassword.value.trim()) {
                    currentPassword.classList.add('is-invalid');
                    isValid = false;
                }

                // Validate new password
                const passwordRegex = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[^\da-zA-Z]).{8,}$/;
                if (!passwordRegex.test(newPassword.value)) {
                    newPassword.classList.add('is-invalid');
                    isValid = false;
                }

                // Validate password confirmation
                if (newPassword.value !== confirmPassword.value) {
                    confirmPassword.classList.add('is-invalid');
                    isValid = false;
                }

                if (isValid) {
                    // Show loading state
                    this.innerHTML = '<span class="spinner-border spinner-border-sm" role="status" aria-hidden="true"></span> Changing...';
                    this.disabled = true;

                    // Submit the form
                    form.submit();
                }
            }); *@

                // Password Change Modal Functionality
                document.getElementById('submitPasswordChange').addEventListener('click', function () {
                    const form = document.getElementById('changePasswordForm');
                    const currentPassword = document.getElementById('currentPassword');
                    const newPassword = document.getElementById('newPassword');
                    const confirmPassword = document.getElementById('confirmPassword');

                    let isValid = true;

                    // Reset validation
                    [currentPassword, newPassword, confirmPassword].forEach(input => {
                        input.classList.remove('is-invalid');
                    });

                    // Validate current password
                    if (!currentPassword.value.trim()) {
                        currentPassword.classList.add('is-invalid');
                        isValid = false;
                    }

                    // ✅ SIMPLIFIED: Validate new password (only length)
                    if (newPassword.value.length < 6) {
                        newPassword.classList.add('is-invalid');
                        isValid = false;
                    }

                    // Validate password confirmation
                    if (newPassword.value !== confirmPassword.value) {
                        confirmPassword.classList.add('is-invalid');
                        isValid = false;
                    }

                    if (isValid) {
                        // Show loading state
                        this.innerHTML = '<span class="spinner-border spinner-border-sm" role="status" aria-hidden="true"></span> Changing...';
                        this.disabled = true;

                        // Submit the form
                        form.submit();
                    }
                });

            // Password match validation in real-time
            document.getElementById('confirmPassword').addEventListener('input', function () {
                const newPassword = document.getElementById('newPassword').value;
                const confirmPassword = this.value;

                if (confirmPassword && newPassword !== confirmPassword) {
                    this.classList.add('is-invalid');
                } else {
                    this.classList.remove('is-invalid');
                }
            });

            // Reset modal when closed
            document.getElementById('changePasswordModal').addEventListener('hidden.bs.modal', function () {
                const form = document.getElementById('changePasswordForm');
                form.reset();

                // Reset validation states
                ['currentPassword', 'newPassword', 'confirmPassword'].forEach(id => {
                    const input = document.getElementById(id);
                    if (input) {
                        input.classList.remove('is-invalid');
                    }
                });

                // Reset submit button
                const submitBtn = document.getElementById('submitPasswordChange');
                submitBtn.innerHTML = 'Change Password';
                submitBtn.disabled = false;
            });
        });
    </script>
}